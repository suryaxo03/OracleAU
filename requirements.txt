# OracleAU — Python dependencies
# Install with: pip install -r requirements.txt

# API
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.8.2

# Data — Stooq via direct HTTP (no pandas_datareader, no yfinance)
requests==2.32.3            # HTTP client for Stooq CSV endpoint
pandas==2.2.2
numpy==1.26.4

# Technical indicators
ta==0.11.0

# ML
scikit-learn==1.5.1
xgboost==2.1.1
joblib==1.4.2
matplotlib==3.9.2          # Required by model_training_v2.py for plots

# Database
pymongo[srv]==4.8.0        # MongoDB Atlas driver (srv = Atlas DNS seedlist)

# Deep learning
# GitHub Actions (ubuntu-latest) + Render use this:
tensorflow==2.17.0

# Apple Silicon (M1/M2/M3) — uncomment locally if needed:
# tensorflow-metal==1.1.0